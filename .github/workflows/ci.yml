name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  backend-ktlint:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: true
      matrix:
        java: [21]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Java
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4
        with:
          cache-key-prefix: ${{ runner.os }}-gradle-${{ hashFiles('gradle.properties', 'gradle/libs.versions.toml', 'settings.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
      - name: Run ktlint
        run: ./gradlew --no-daemon :backend:app:ktlintCheck --console=plain

  backend-compile:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: true
      matrix:
        java: [21]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Java
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4
        with:
          cache-key-prefix: ${{ runner.os }}-gradle-${{ hashFiles('gradle.properties', 'gradle/libs.versions.toml', 'settings.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
      - name: Compile backend
        run: ./gradlew --no-daemon :backend:app:compileKotlin --console=plain

  backend-release-critical-routes:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: true
      matrix:
        java: [21]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Java
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4
        with:
          cache-key-prefix: ${{ runner.os }}-gradle-${{ hashFiles('gradle.properties', 'gradle/libs.versions.toml', 'settings.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
      - name: Verify Docker availability (required for Testcontainers)
        run: docker info
      - name: Run guest venue platform route and security tests
        env:
          TZ: UTC
        run: |
          echo "CI timezone contract: $TZ"
          JAVA_TOOL_OPTIONS=-Dapi.version=1.44 _JAVA_OPTIONS=-Xmx4g \
            ./gradlew --no-daemon --max-workers=1 :backend:app:test \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestVenueRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestOrderRoutesTest" \
            --tests "com.hookah.platform.backend.telegram.db.GuestOrderIdempotencyFingerprintTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestVisitRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestTableResolveRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.db.GuestTableContextActivationRepositoryTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.db.GuestTableContextTeardownRepositoryTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.db.PlatformGuestTableMutationCoordinatorTest" \
            --tests "com.hookah.platform.backend.telegram.PlatformGuestQrPendingConfirmationStoreTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestTabsRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestStaffCallRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.shift.ShiftExtensionRoutesTest" \
            --tests "com.hookah.platform.backend.support.SupportTicketRoutesTest" \
            --tests "com.hookah.platform.backend.support.SupportThreadReadRepositoryTest" \
            --tests "com.hookah.platform.backend.telegram.BookingMessageStaffChatNotifierTest" \
            --tests "com.hookah.platform.backend.telegram.StaffChatNotifierTest" \
            --tests "com.hookah.platform.backend.telegram.TelegramOutboxWorkerTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.VenueOrderRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.promotions.VenuePromotionRoutesTest" \
            --tests "com.hookah.platform.backend.telegram.db.VenuePromotionRepositoryTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.menu.VenueMenuRepositoryTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.VenueMenuRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.AuditLogRepositoryTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.VenueStaffRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.staff.VenueStaffRepositoryTest" \
            --tests "com.hookah.platform.backend.platform.PlatformVenueRoutesTest" \
            --tests "com.hookah.platform.backend.platform.PlatformRoutesTest" \
            --tests "com.hookah.platform.backend.platform.PlatformVenueRepositoryTest" \
            --tests "com.hookah.platform.backend.platform.PlatformVenueMemberRepositoryTest" \
            --tests "com.hookah.platform.backend.platform.VenueOwnerAccountRepositoryTest" \
            --tests "com.hookah.platform.backend.telegram.db.VenueConnectionRequestRepositoryTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.VenueOnboardingRoutesTest" \
            --tests "com.hookah.platform.backend.platform.PlatformOnboardingRoutesTest" \
            --tests "com.hookah.platform.backend.telegram.TelegramBotRouterVenueConnectionRequestFlowTest" \
            --tests "com.hookah.platform.backend.telegram.TelegramBotRouterTableTokenTest" \
            --tests "com.hookah.platform.backend.telegram.TelegramKeyboardsTest" \
            --console=plain
      - name: Assert route and security test results
        if: ${{ !cancelled() }}
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from xml.etree import ElementTree

          report_dir = Path("backend/app/build/test-results/test")
          expected_classes = [
              "com.hookah.platform.backend.miniapp.guest.GuestVenueRoutesTest",
              "com.hookah.platform.backend.miniapp.guest.GuestOrderRoutesTest",
              "com.hookah.platform.backend.telegram.db.GuestOrderIdempotencyFingerprintTest",
              "com.hookah.platform.backend.miniapp.guest.GuestVisitRoutesTest",
              "com.hookah.platform.backend.miniapp.guest.GuestTableResolveRoutesTest",
              "com.hookah.platform.backend.miniapp.guest.db.GuestTableContextActivationRepositoryTest",
              "com.hookah.platform.backend.miniapp.guest.db.GuestTableContextTeardownRepositoryTest",
              "com.hookah.platform.backend.miniapp.guest.db.PlatformGuestTableMutationCoordinatorTest",
              "com.hookah.platform.backend.telegram.PlatformGuestQrPendingConfirmationStoreTest",
              "com.hookah.platform.backend.miniapp.guest.GuestTabsRoutesTest",
              "com.hookah.platform.backend.miniapp.guest.GuestStaffCallRoutesTest",
              "com.hookah.platform.backend.miniapp.shift.ShiftExtensionRoutesTest",
              "com.hookah.platform.backend.support.SupportTicketRoutesTest",
              "com.hookah.platform.backend.support.SupportThreadReadRepositoryTest",
              "com.hookah.platform.backend.telegram.BookingMessageStaffChatNotifierTest",
              "com.hookah.platform.backend.telegram.StaffChatNotifierTest",
              "com.hookah.platform.backend.telegram.TelegramOutboxWorkerTest",
              "com.hookah.platform.backend.miniapp.venue.VenueOrderRoutesTest",
              "com.hookah.platform.backend.miniapp.venue.promotions.VenuePromotionRoutesTest",
              "com.hookah.platform.backend.telegram.db.VenuePromotionRepositoryTest",
              "com.hookah.platform.backend.miniapp.venue.menu.VenueMenuRepositoryTest",
              "com.hookah.platform.backend.miniapp.venue.VenueMenuRoutesTest",
              "com.hookah.platform.backend.miniapp.venue.AuditLogRepositoryTest",
              "com.hookah.platform.backend.miniapp.venue.VenueStaffRoutesTest",
              "com.hookah.platform.backend.miniapp.venue.staff.VenueStaffRepositoryTest",
              "com.hookah.platform.backend.platform.PlatformVenueRoutesTest",
              "com.hookah.platform.backend.platform.PlatformRoutesTest",
              "com.hookah.platform.backend.platform.PlatformVenueRepositoryTest",
              "com.hookah.platform.backend.platform.PlatformVenueMemberRepositoryTest",
              "com.hookah.platform.backend.platform.VenueOwnerAccountRepositoryTest",
              "com.hookah.platform.backend.telegram.db.VenueConnectionRequestRepositoryTest",
              "com.hookah.platform.backend.miniapp.venue.VenueOnboardingRoutesTest",
              "com.hookah.platform.backend.platform.PlatformOnboardingRoutesTest",
              "com.hookah.platform.backend.telegram.TelegramBotRouterVenueConnectionRequestFlowTest",
              "com.hookah.platform.backend.telegram.TelegramBotRouterTableTokenTest",
              "com.hookah.platform.backend.telegram.TelegramKeyboardsTest",
          ]
          required_minimums = {
              "com.hookah.platform.backend.support.SupportTicketRoutesTest": 13,
              "com.hookah.platform.backend.support.SupportThreadReadRepositoryTest": 21,
              "com.hookah.platform.backend.telegram.BookingMessageStaffChatNotifierTest": 5,
              "com.hookah.platform.backend.telegram.StaffChatNotifierTest": 39,
              "com.hookah.platform.backend.telegram.TelegramOutboxWorkerTest": 8,
              "com.hookah.platform.backend.miniapp.guest.GuestOrderRoutesTest": 61,
              "com.hookah.platform.backend.telegram.db.GuestOrderIdempotencyFingerprintTest": 7,
              "com.hookah.platform.backend.miniapp.venue.menu.VenueMenuRepositoryTest": 54,
              "com.hookah.platform.backend.miniapp.venue.VenueMenuRoutesTest": 46,
              "com.hookah.platform.backend.platform.PlatformVenueRoutesTest": 13,
              "com.hookah.platform.backend.platform.PlatformRoutesTest": 9,
              "com.hookah.platform.backend.platform.PlatformVenueRepositoryTest": 1,
              "com.hookah.platform.backend.platform.PlatformVenueMemberRepositoryTest": 1,
              "com.hookah.platform.backend.platform.VenueOwnerAccountRepositoryTest": 9,
              "com.hookah.platform.backend.telegram.db.VenueConnectionRequestRepositoryTest": 11,
              "com.hookah.platform.backend.miniapp.venue.VenueOnboardingRoutesTest": 8,
              "com.hookah.platform.backend.platform.PlatformOnboardingRoutesTest": 15,
              "com.hookah.platform.backend.telegram.TelegramBotRouterVenueConnectionRequestFlowTest": 18,
              "com.hookah.platform.backend.telegram.TelegramBotRouterTableTokenTest": 553,
              "com.hookah.platform.backend.telegram.TelegramKeyboardsTest": 169,
          }
          required_test_names = {
              "com.hookah.platform.backend.support.SupportTicketRoutesTest": {
                  "confirmed Platform Guest surface contract rejects cross inbox opens without raw marker mutation or facts",
              },
              "com.hookah.platform.backend.support.SupportThreadReadRepositoryTest": {
                  "NULL author venue chat participates in every unread projection and clears on exact open",
                  "NULL author support and unrelated threads stay excluded and unauthorized actors get no count facts",
              },
              "com.hookah.platform.backend.telegram.TelegramBotRouterTableTokenTest": {
                  "bot booking create uses zone A once when resolver would next return zone B",
              },
          }
          totals = {key: 0 for key in ("tests", "skipped", "failures", "errors")}
          problems = []
          for class_name in expected_classes:
              report = report_dir / f"TEST-{class_name}.xml"
              if not report.is_file():
                  problems.append(f"missing JUnit XML: {report}")
                  continue
              suite = ElementTree.parse(report).getroot()
              counts = {key: int(suite.attrib.get(key, "0")) for key in totals}
              for key, value in counts.items():
                  totals[key] += value
              minimum_tests = required_minimums.get(class_name, 1)
              if counts["tests"] < minimum_tests:
                  problems.append(
                      f"incomplete suite: {class_name} "
                      f"required={minimum_tests} actual={counts['tests']}"
                  )
              if any(counts[key] for key in ("skipped", "failures", "errors")):
                  problems.append(f"non-passing suite {class_name}: {counts}")
              actual_names = [
                  (case.attrib.get("name") or "").removesuffix("()")
                  for case in suite.iter("testcase")
              ]
              for required_name in required_test_names.get(class_name, set()):
                  occurrences = actual_names.count(required_name)
                  if occurrences != 1:
                      problems.append(
                          f"missing or duplicate mandatory testcase: {class_name} "
                          f"name={required_name!r} actual={occurrences}"
                      )

          executed = totals["tests"] - totals["skipped"]
          print(
              "Route/security JUnit: "
              f"discovered={totals['tests']} executed={executed} "
              f"skipped={totals['skipped']} failures={totals['failures']} errors={totals['errors']}"
          )
          if problems:
              raise SystemExit("\n".join(problems))
          PY
      - name: Run mandatory PostgreSQL menu mutation gate
        if: ${{ !cancelled() }}
        run: |
          JAVA_TOOL_OPTIONS=-Dapi.version=1.44 _JAVA_OPTIONS=-Xmx4g \
            ./gradlew --no-daemon --max-workers=1 :backend:app:test \
            --tests "com.hookah.platform.backend.miniapp.venue.menu.VenueMenuOptionNormalizationConcurrencyPostgresTest" \
            --console=plain
      - name: Assert mandatory PostgreSQL menu mutation results
        if: ${{ !cancelled() }}
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from xml.etree import ElementTree

          report = Path(
              "backend/app/build/test-results/test/"
              "TEST-com.hookah.platform.backend.miniapp.venue.menu."
              "VenueMenuOptionNormalizationConcurrencyPostgresTest.xml"
          )
          if not report.is_file():
              raise SystemExit(f"missing JUnit XML: {report}")
          suite = ElementTree.parse(report).getroot()
          counts = {
              key: int(suite.attrib.get(key, "0"))
              for key in ("tests", "skipped", "failures", "errors")
          }
          executed = counts["tests"] - counts["skipped"]
          print(
              f"PostgreSQL menu mutation JUnit: xml={report} "
              f"discovered={counts['tests']} executed={executed} "
              f"skipped={counts['skipped']} failures={counts['failures']} "
              f"errors={counts['errors']} required=44"
          )
          if counts["tests"] < 44:
              raise SystemExit(
                  "incomplete PostgreSQL menu mutation matrix: "
                  f"required=44 actual={counts['tests']}"
              )
          if any(counts[key] for key in ("skipped", "failures", "errors")):
              raise SystemExit(f"PostgreSQL menu mutation gate failed: {counts}")
          PY
      - name: Run mandatory PostgreSQL integrity and concurrency gates
        if: ${{ !cancelled() }}
        run: |
          JAVA_TOOL_OPTIONS=-Dapi.version=1.44 _JAVA_OPTIONS=-Xmx4g \
            ./gradlew --no-daemon --max-workers=1 :backend:app:test \
            --tests "com.hookah.platform.backend.miniapp.guest.db.GuestTableContextActivationPostgresTest" \
            --tests "com.hookah.platform.backend.telegram.db.PromotionConfigurationConcurrencyPostgresTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.staff.VenueStaffMutationConcurrencyPostgresTest" \
            --tests "com.hookah.platform.backend.telegram.db.VenueOnboardingConcurrencyPostgresTest" \
            --tests "com.hookah.platform.backend.telegram.db.GuestOrderIdempotencyConcurrencyPostgresTest" \
            --tests "com.hookah.platform.backend.support.BookingConversationConcurrencyPostgresTest" \
            --tests "com.hookah.platform.backend.support.BookingMessageIdempotencyPostgresTest" \
            --tests "com.hookah.platform.backend.support.SupportThreadReadConcurrencyPostgresTest" \
            --console=plain
      - name: Assert mandatory PostgreSQL integrity and concurrency results
        if: ${{ !cancelled() }}
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from xml.etree import ElementTree

          report_dir = Path("backend/app/build/test-results/test")
          required_suites = [
              (
                  "activation",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.guest.db."
                      "GuestTableContextActivationPostgresTest.xml"
                  ),
                  8,
              ),
              (
                  "support-thread read canonical parent lock order",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "SupportThreadReadConcurrencyPostgresTest.xml"
                  ),
                  6,
              ),
              (
                  "promotion lifecycle/configuration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.telegram.db."
                      "PromotionConfigurationConcurrencyPostgresTest.xml"
                  ),
                  14,
              ),
              (
                  "staff membership concurrency",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.venue.staff."
                      "VenueStaffMutationConcurrencyPostgresTest.xml"
                  ),
                  2,
              ),
              (
                  "guest order idempotency concurrency",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.telegram.db."
                      "GuestOrderIdempotencyConcurrencyPostgresTest.xml"
                  ),
                  9,
              ),
              (
                  "venue onboarding serialization and create-link",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.telegram.db."
                      "VenueOnboardingConcurrencyPostgresTest.xml"
                  ),
                  7,
              ),
              (
                  "booking conversation canonical row lock",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "BookingConversationConcurrencyPostgresTest.xml"
                  ),
                  1,
              ),
              (
                  "booking Mini App message idempotency and transactional outbox",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "BookingMessageIdempotencyPostgresTest.xml"
                  ),
                  17,
              ),
          ]
          problems = []
          required_test_names = {
              "PostgreSQL NULL author venue chat is unread until locked exact open and preserves another actor marker",
          }
          for label, report, minimum_tests in required_suites:
              if not report.is_file():
                  problems.append(f"missing JUnit XML: {report}")
                  continue
              suite = ElementTree.parse(report).getroot()
              counts = {
                  key: int(suite.attrib.get(key, "0"))
                  for key in ("tests", "skipped", "failures", "errors")
              }
              executed = counts["tests"] - counts["skipped"]
              print(
                  f"PostgreSQL {label} JUnit: xml={report} "
                  f"discovered={counts['tests']} executed={executed} "
                  f"skipped={counts['skipped']} failures={counts['failures']} "
                  f"errors={counts['errors']} required={minimum_tests}"
              )
              if counts["tests"] < minimum_tests:
                  problems.append(
                      f"incomplete PostgreSQL {label} matrix: "
                      f"required={minimum_tests} actual={counts['tests']}"
                  )
              if any(counts[key] for key in ("skipped", "failures", "errors")):
                  problems.append(f"PostgreSQL {label} gate failed: {counts}")
              if report.name.endswith("SupportThreadReadConcurrencyPostgresTest.xml"):
                  actual_names = [
                      (case.attrib.get("name") or "").removesuffix("()")
                      for case in suite.iter("testcase")
                  ]
                  for required_name in required_test_names:
                      occurrences = actual_names.count(required_name)
                      if occurrences != 1:
                          problems.append(
                              "missing or duplicate mandatory PostgreSQL testcase: "
                              f"name={required_name!r} actual={occurrences}"
                          )

          if problems:
              raise SystemExit("\n".join(problems))
          PY

  backend-venue-booking-rbac:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: true
      matrix:
        java: [21]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Java
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4
        with:
          cache-key-prefix: ${{ runner.os }}-gradle-${{ hashFiles('gradle.properties', 'gradle/libs.versions.toml', 'settings.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
      - name: Verify Docker availability (required for Testcontainers)
        run: docker info
      - name: Run venue booking RBAC tests
        env:
          TZ: UTC
        run: |
          echo "CI timezone contract: $TZ"
          ./gradlew --no-daemon :backend:app:test \
            --tests "com.hookah.platform.backend.booking.BookingDisplayLabelTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestBookingRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.BookingReminderWorkerTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.VenueBookingRoutesTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.VenueRbacRoutesTest" \
            --tests "com.hookah.platform.backend.support.BookingConversationRoutesTest" \
            --tests "com.hookah.platform.backend.support.BookingConversationRepositoryTest" \
            --console=plain
      - name: Assert venue booking and conversation test results
        if: ${{ !cancelled() }}
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from xml.etree import ElementTree

          report_dir = Path("backend/app/build/test-results/test")
          required_suites = [
              (
                  "booking display label",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.booking."
                      "BookingDisplayLabelTest.xml"
                  ),
                  2,
              ),
              (
                  "guest booking routes",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.guest."
                      "GuestBookingRoutesTest.xml"
                  ),
                  10,
              ),
              (
                  "booking reminder worker",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.guest."
                      "BookingReminderWorkerTest.xml"
                  ),
                  1,
              ),
              (
                  "venue booking routes",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.venue."
                      "VenueBookingRoutesTest.xml"
                  ),
                  10,
              ),
              (
                  "venue RBAC routes",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.venue."
                      "VenueRbacRoutesTest.xml"
                  ),
                  36,
              ),
              (
                  "booking conversation routes",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                  "BookingConversationRoutesTest.xml"
                  ),
                  8,
              ),
              (
                  "booking conversation repository",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "BookingConversationRepositoryTest.xml"
                  ),
                  13,
              ),
          ]
          required_test_names = {
              "TEST-com.hookah.platform.backend.miniapp.guest.GuestBookingRoutesTest.xml": {
                  "guest attendance missing timezone uses Moscow once for response staff and deadline under UTC",
                  "guest attendance Honolulu timezone wins once for response staff and deadline under UTC",
              },
              "TEST-com.hookah.platform.backend.miniapp.venue.VenueBookingRoutesTest.xml": {
                  "valid Honolulu timezone wins over UTC host for persisted deadline reminder and notification",
              },
              "TEST-com.hookah.platform.backend.support.BookingConversationRoutesTest.xml": {
                  "ordinary Guest surface contract rejects cross inbox opens without raw marker mutation or message facts",
              },
          }
          problems = []
          for label, report, minimum in required_suites:
              if not report.is_file():
                  problems.append(f"missing JUnit XML: {report}")
                  continue
              suite = ElementTree.parse(report).getroot()
              counts = {
                  key: int(suite.attrib.get(key, "0"))
                  for key in ("tests", "skipped", "failures", "errors")
              }
              print(f"{label}: xml={report} minimum={minimum} counts={counts}")
              if counts["tests"] < minimum:
                  problems.append(
                      f"incomplete {label}: required={minimum} actual={counts['tests']}"
                  )
              if any(counts[key] for key in ("skipped", "failures", "errors")):
                  problems.append(f"non-passing {label}: {counts}")
              actual_names = [
                  (case.attrib.get("name") or "").removesuffix("()")
                  for case in suite.iter("testcase")
              ]
              for required_name in required_test_names.get(report.name, set()):
                  occurrences = actual_names.count(required_name)
                  if occurrences != 1:
                      problems.append(
                          "missing or duplicate mandatory booking testcase: "
                          f"report={report.name} name={required_name!r} actual={occurrences}"
                      )

          if problems:
              raise SystemExit("\n".join(problems))
          PY

  backend-telegram-lightweight:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: true
      matrix:
        java: [21]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Java
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4
        with:
          cache-key-prefix: ${{ runner.os }}-gradle-${{ hashFiles('gradle.properties', 'gradle/libs.versions.toml', 'settings.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
      - name: Run Telegram keyboard payload notifier tests
        run: |
          ./gradlew --no-daemon :backend:app:test \
            --tests "com.hookah.platform.backend.telegram.TelegramKeyboardsTest" \
            --tests "com.hookah.platform.backend.telegram.SendMessagePayloadTest" \
            --tests "com.hookah.platform.backend.telegram.TelegramBotRouterLinkCommandTest" \
            --console=plain

  backend-migration-sanity:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: true
      matrix:
        java: [21]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Java
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4
        with:
          cache-key-prefix: ${{ runner.os }}-gradle-${{ hashFiles('gradle.properties', 'gradle/libs.versions.toml', 'settings.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties') }}
      - name: Verify Docker availability (required for Testcontainers)
        run: docker info
      - name: Run migration sanity tests
        run: |
          JAVA_TOOL_OPTIONS=-Dapi.version=1.44 _JAVA_OPTIONS=-Xmx4g \
            ./gradlew --no-daemon --max-workers=1 :backend:app:test \
            --tests "com.hookah.platform.backend.telegram.TelegramDialogStateMigrationTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.TableSessionsMigrationV28PostgresTest" \
            --tests "com.hookah.platform.backend.miniapp.venue.AuditLogTargetMigrationH2Test" \
            --tests "com.hookah.platform.backend.miniapp.venue.AuditLogTargetMigrationPostgresTest" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestBatchIdempotencyFingerprintMigrationH2Test" \
            --tests "com.hookah.platform.backend.miniapp.guest.GuestBatchIdempotencyFingerprintMigrationPostgresTest" \
            --tests "com.hookah.platform.backend.support.BookingThreadIntegrityMigrationH2Test" \
            --tests "com.hookah.platform.backend.support.BookingThreadIntegrityMigrationPostgresTest" \
            --tests "com.hookah.platform.backend.support.BookingAuditReferencePolicyTest" \
            --tests "com.hookah.platform.backend.support.BookingMessageIdempotencyMigrationH2Test" \
            --tests "com.hookah.platform.backend.support.BookingMessageIdempotencyMigrationPostgresTest" \
            --tests "com.hookah.platform.backend.support.SupportThreadReadCursorMigrationH2Test" \
            --tests "com.hookah.platform.backend.support.SupportThreadReadCursorMigrationPostgresTest" \
            --tests "com.hookah.platform.backend.support.BookingThreadIntegrityMigrationConcurrencyPostgresTest" \
            --console=plain
      - name: Assert mandatory migration test results
        if: ${{ !cancelled() }}
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from xml.etree import ElementTree

          report_dir = Path("backend/app/build/test-results/test")
          required_suites = [
              (
                  "H2 Telegram dialog-state migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.telegram."
                      "TelegramDialogStateMigrationTest.xml"
                  ),
                  1,
              ),
              (
                  "PostgreSQL table-session V28 migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.guest."
                      "TableSessionsMigrationV28PostgresTest.xml"
                  ),
                  1,
              ),
              (
                  "H2 audit target migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.venue."
                      "AuditLogTargetMigrationH2Test.xml"
                  ),
                  2,
              ),
              (
                  "PostgreSQL audit target migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.venue."
                      "AuditLogTargetMigrationPostgresTest.xml"
                  ),
                  2,
              ),
              (
                  "H2 guest idempotency fingerprint migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.guest."
                      "GuestBatchIdempotencyFingerprintMigrationH2Test.xml"
                  ),
                  2,
              ),
              (
                  "PostgreSQL guest idempotency fingerprint migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.miniapp.guest."
                      "GuestBatchIdempotencyFingerprintMigrationPostgresTest.xml"
                  ),
                  2,
              ),
              (
                  "H2 booking thread integrity migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                  "BookingThreadIntegrityMigrationH2Test.xml"
                  ),
                  42,
              ),
              (
                  "PostgreSQL booking thread integrity migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "BookingThreadIntegrityMigrationPostgresTest.xml"
                  ),
                  47,
              ),
              (
                  "booking audit recursive-key PG/H2/preflight predicate parity",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "BookingAuditReferencePolicyTest.xml"
                  ),
                  6,
              ),
              (
                  "H2 booking Mini App message idempotency migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                  "BookingMessageIdempotencyMigrationH2Test.xml"
                  ),
                  5,
              ),
              (
                  "PostgreSQL booking Mini App message idempotency migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                  "BookingMessageIdempotencyMigrationPostgresTest.xml"
                  ),
                  5,
              ),
              (
                  "H2 support-thread read message cursor migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "SupportThreadReadCursorMigrationH2Test.xml"
                  ),
                  4,
              ),
              (
                  "PostgreSQL support-thread read message cursor migration",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "SupportThreadReadCursorMigrationPostgresTest.xml"
                  ),
                  4,
              ),
              (
                  "PostgreSQL booking thread migration lock serialization",
                  report_dir / (
                      "TEST-com.hookah.platform.backend.support."
                      "BookingThreadIntegrityMigrationConcurrencyPostgresTest.xml"
                  ),
                  2,
              ),
          ]
          problems = []
          for label, report, minimum_tests in required_suites:
              if not report.is_file():
                  problems.append(f"missing JUnit XML: {report}")
                  continue
              suite = ElementTree.parse(report).getroot()
              counts = {
                  key: int(suite.attrib.get(key, "0"))
                  for key in ("tests", "skipped", "failures", "errors")
              }
              executed = counts["tests"] - counts["skipped"]
              print(
                  f"{label} JUnit: xml={report} "
                  f"discovered={counts['tests']} executed={executed} "
                  f"skipped={counts['skipped']} failures={counts['failures']} "
                  f"errors={counts['errors']} required={minimum_tests}"
              )
              if counts["tests"] < minimum_tests:
                  problems.append(
                      f"incomplete {label} matrix: "
                      f"required={minimum_tests} actual={counts['tests']}"
                  )
              if any(counts[key] for key in ("skipped", "failures", "errors")):
                  problems.append(f"{label} gate failed: {counts}")

          if problems:
              raise SystemExit("\n".join(problems))
          PY

  backend:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: ${{ always() }}
    needs:
      - backend-ktlint
      - backend-compile
      - backend-release-critical-routes
      - backend-venue-booking-rbac
      - backend-telegram-lightweight
      - backend-migration-sanity
    steps:
      - name: Aggregate backend checks
        run: |
          echo "backend-ktlint result: ${{ needs.backend-ktlint.result }}"
          echo "backend-compile result: ${{ needs.backend-compile.result }}"
          echo "backend-release-critical-routes result: ${{ needs.backend-release-critical-routes.result }}"
          echo "backend-venue-booking-rbac result: ${{ needs.backend-venue-booking-rbac.result }}"
          echo "backend-telegram-lightweight result: ${{ needs.backend-telegram-lightweight.result }}"
          echo "backend-migration-sanity result: ${{ needs.backend-migration-sanity.result }}"

          if [[ "${{ needs.backend-ktlint.result }}" != "success" || \
                "${{ needs.backend-compile.result }}" != "success" || \
                "${{ needs.backend-release-critical-routes.result }}" != "success" || \
                "${{ needs.backend-venue-booking-rbac.result }}" != "success" || \
                "${{ needs.backend-telegram-lightweight.result }}" != "success" || \
                "${{ needs.backend-migration-sanity.result }}" != "success" ]]; then
            echo "backend checks failed"
            exit 1
          fi

          echo "backend checks passed"

  compose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Create CI compose env
        run: |
          cat > .env <<'EOF'
          POSTGRES_DB=hookah
          POSTGRES_USER=hookah
          POSTGRES_PASSWORD=ci-placeholder-password
          EOF
      - name: Validate Docker Compose config
        run: docker compose config --quiet

  miniapp:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: true
      matrix:
        node: [20]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node }}
      - name: Detect miniapp lockfile
        id: miniapp-lockfile
        run: |
          cd miniapp
          lockfiles=()
          if [ -f pnpm-lock.yaml ]; then
            lockfiles+=("pnpm-lock.yaml")
          fi
          if [ -f package-lock.json ]; then
            lockfiles+=("package-lock.json")
          fi
          if [ -f yarn.lock ]; then
            lockfiles+=("yarn.lock")
          fi
          if [ ${#lockfiles[@]} -eq 0 ]; then
            echo "::error::No lockfile found in miniapp/. Expected pnpm-lock.yaml, package-lock.json, or yarn.lock."
            exit 1
          fi
          if [ ${#lockfiles[@]} -gt 1 ]; then
            echo "::warning::Multiple lockfiles detected in miniapp/: ${lockfiles[*]}. Using priority pnpm > npm > yarn."
          fi
          if [ -f pnpm-lock.yaml ]; then
            echo "package_manager=pnpm" >> "$GITHUB_OUTPUT"
          elif [ -f package-lock.json ]; then
            echo "package_manager=npm" >> "$GITHUB_OUTPUT"
          else
            echo "package_manager=yarn" >> "$GITHUB_OUTPUT"
          fi
      - name: Set up pnpm
        if: steps.miniapp-lockfile.outputs.package_manager == 'pnpm'
        uses: pnpm/action-setup@c5ba7f7862a0f64c1b1a05fbac13e0b8e86ba08c # v4
        with:
          version: 9
      - name: Install dependencies
        working-directory: miniapp
        run: |
          case "${{ steps.miniapp-lockfile.outputs.package_manager }}" in
            pnpm)
              pnpm install --frozen-lockfile
              ;;
            npm)
              npm ci
              ;;
            yarn)
              corepack enable
              yarn install --frozen-lockfile
              ;;
            *)
              echo "::error::Unknown package manager."
              exit 1
              ;;
          esac
      - name: Build miniapp
        working-directory: miniapp
        run: |
          case "${{ steps.miniapp-lockfile.outputs.package_manager }}" in
            pnpm)
              pnpm build
              ;;
            npm)
              npm run build
              ;;
            yarn)
              yarn build
              ;;
            *)
              echo "::error::Unknown package manager."
              exit 1
              ;;
          esac

  miniapp-e2e-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: true
      matrix:
        node: [20]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node }}
      - name: Install dependencies
        working-directory: miniapp
        run: npm ci
      - name: Install Playwright Chromium
        working-directory: miniapp
        run: npx playwright install --with-deps chromium
      - name: Run full Mini App browser smoke with structured JSON report
        working-directory: miniapp
        env:
          CI: "1"
          TZ: UTC
          MINIAPP_E2E_PORT: "5174"
          PLAYWRIGHT_JSON_OUTPUT_FILE: playwright-results.json
        run: npm run e2e:smoke -- --reporter=list,json
      - name: Assert full Mini App browser smoke results
        if: ${{ !cancelled() }}
        working-directory: miniapp
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          report_path = Path("playwright-results.json")
          minimum_executed = 216
          if not report_path.is_file():
              raise SystemExit(f"missing Playwright JSON report: {report_path}")

          try:
              report = json.loads(report_path.read_text(encoding="utf-8"))
          except (OSError, UnicodeError, json.JSONDecodeError) as error:
              raise SystemExit(f"invalid Playwright JSON report {report_path}: {error}") from error

          stats = report.get("stats")
          if not isinstance(stats, dict):
              raise SystemExit(f"missing Playwright stats object: {report_path}")

          required_stats = ("expected", "unexpected", "flaky", "skipped")
          invalid_stats = {
              key: stats.get(key)
              for key in required_stats
              if (
                  not isinstance(stats.get(key), int)
                  or isinstance(stats.get(key), bool)
                  or stats.get(key) < 0
              )
          }
          if invalid_stats:
              raise SystemExit(f"invalid Playwright stats in {report_path}: {invalid_stats}")

          suites = report.get("suites")
          if not isinstance(suites, list):
              raise SystemExit(f"invalid Playwright suites array: {report_path}")

          def iter_specs(suite, inherited_file=None):
              suite_file = suite.get("file", inherited_file)
              for spec in suite.get("specs", []):
                  yield spec.get("file", suite_file), spec
              for child in suite.get("suites", []):
                  yield from iter_specs(child, suite_file)

          required_specs = {
              (
                  "booking-label-parity.spec.ts",
                  "shared booking label fixture stays aligned with the production TypeScript helper",
              ): 1,
              (
                  "booking-label-parity.spec.ts",
                  "authoritative booking labels do not change with the browser timezone",
              ): 1,
              (
                  "guest-smoke.spec.ts",
                  "guest booking message keeps its client message id for unchanged manual retry after a lost response",
              ): 1,
              (
                  "guest-smoke.spec.ts",
                  "guest booking message skips the staff alert when staff chat is not configured",
              ): 1,
              (
                  "guest-smoke.spec.ts",
                  "guest conversation deep link rejects support ticket without clearing unread marker",
              ): 1,
              (
                  "guest-smoke.spec.ts",
                  "guest support deep links reject booking and venue chats without clearing unread markers",
              ): 1,
              (
                  "guest-smoke.spec.ts",
                  "guest exact surface deep links clear only the opened thread unread marker",
              ): 1,
              (
                  "guest-smoke.spec.ts",
                  "guest ignores a late detail response from the previous surface",
              ): 1,
          }

          try:
              reported_specs = [item for suite in suites for item in iter_specs(suite)]
              reported_tests = [
                  test
                  for _, spec in reported_specs
                  for test in spec.get("tests", [])
              ]
              outcome_counts = {
                  key: sum(test.get("status") == key for test in reported_tests)
                  for key in required_stats
              }
              unknown_outcomes = [
                  test.get("status")
                  for test in reported_tests
                  if test.get("status") not in required_stats
              ]
          except (AttributeError, TypeError) as error:
              raise SystemExit(f"invalid Playwright suite structure: {error}") from error

          required_spec_problems = []
          for (required_file, required_title), required_count in required_specs.items():
              matching_tests = [
                  test
                  for file_name, spec in reported_specs
                  if (
                      isinstance(file_name, str)
                      and Path(file_name).name == required_file
                      and spec.get("title") == required_title
                  )
                  for test in spec.get("tests", [])
              ]
              if len(matching_tests) != required_count:
                  required_spec_problems.append(
                      "missing or duplicate mandatory Playwright spec: "
                      f"file={required_file} title={required_title!r} "
                      f"required={required_count} actual={len(matching_tests)}"
                  )
              elif matching_tests[0].get("status") != "expected":
                  required_spec_problems.append(
                      "non-passing mandatory Playwright spec: "
                      f"file={required_file} title={required_title!r} "
                      f"status={matching_tests[0].get('status')!r}"
                  )

          if unknown_outcomes:
              raise SystemExit(f"unknown Playwright outcomes: {unknown_outcomes}")
          stats_outcomes = {key: stats[key] for key in required_stats}
          if outcome_counts != stats_outcomes:
              raise SystemExit(
                  f"Playwright stats/suite mismatch: stats={stats_outcomes} "
                  f"suite_outcomes={outcome_counts}"
              )

          expected = stats["expected"]
          unexpected = stats["unexpected"]
          failures = unexpected
          flaky = stats["flaky"]
          skipped = stats["skipped"]
          executed = expected + unexpected + flaky
          runner_errors = report.get("errors")
          if not isinstance(runner_errors, list):
              raise SystemExit(f"invalid Playwright errors array: {report_path}")

          missing_results = sum(
              test["status"] != "skipped" and not test.get("results")
              for test in reported_tests
          )
          non_passing_expectations = sum(
              test["status"] != "skipped" and test.get("expectedStatus") != "passed"
              for test in reported_tests
          )
          failed_attempts = sum(
              result.get("status") != "passed"
              for test in reported_tests
              if test["status"] != "skipped"
              for result in test.get("results", [])
          )

          print(
              f"Playwright JSON: report={report_path} executed={executed} "
              f"expected={expected} failures={failures} flaky={flaky} "
              f"skipped={skipped} runner_errors={len(runner_errors)} "
              f"failed_attempts={failed_attempts} "
              f"required_executed={minimum_executed}"
          )

          problems = []
          problems.extend(required_spec_problems)
          if executed < minimum_executed:
              problems.append(
                  f"incomplete Mini App smoke: required={minimum_executed} actual={executed}"
              )
          if failures:
              problems.append(f"failed Playwright tests: {failures}")
          if flaky:
              problems.append(f"flaky Playwright outcomes: {flaky}")
          if skipped:
              problems.append(f"skipped Playwright tests: {skipped}")
          if runner_errors:
              problems.append(f"Playwright runner errors: {len(runner_errors)}")
          if missing_results:
              problems.append(f"Playwright tests without execution results: {missing_results}")
          if non_passing_expectations:
              problems.append(
                  f"Playwright tests with non-passing expected status: {non_passing_expectations}"
              )
          if failed_attempts:
              problems.append(f"failed Playwright attempts: {failed_attempts}")
          if problems:
              raise SystemExit("\n".join(problems))
          PY

  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: true
      matrix:
        target: [backend]
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Build backend image
        run: docker build -f backend/Dockerfile .
